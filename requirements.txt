# Core — always required
anthropic>=0.25.0

# Optional — only needed for the document formats you use
pdfplumber>=0.10.0      # for .pdf input
python-docx>=1.1.0      # for .docx input

# Dev / testing
pytest>=8.0.0

# Uncomment to use OpenAI instead of Anthropic:
# openai>=1.0.0
